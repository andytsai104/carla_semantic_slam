[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "carla-semantic-slam"
version = "0.1.0"
description = "CARLA RGB-D semantic mapping pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
